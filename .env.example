# ===========================================
# OMNI-DATA - Variáveis de Ambiente
# Copie este arquivo para .env e preencha
# ===========================================

# Banco de Dados
DB_HOST=db
DB_NAME=omni-data
DB_USER=invent_user
DB_PASSWORD=troque_esta_senha
DB_ROOT_PASSWORD=troque_esta_senha_root

# Servidor
PORT=3000
NODE_ENV=production
PUBLIC_BACKEND_URL=https://seu-dominio.com

# Segurança — gere valores únicos para produção:
# node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=troque_por_valor_aleatorio_longo
INTERNAL_API_KEY=troque_por_valor_aleatorio_longo
